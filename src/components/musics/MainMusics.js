import React from 'react';
import AddMusic from './AddMusic';
import BookList from './MusicList';

const MainMusics = () => (
  <div className="mainbook__container">
    <AddMusic />
    <BookList />
  </div>
);

export default MainMusics;
